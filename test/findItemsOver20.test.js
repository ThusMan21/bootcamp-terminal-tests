import assert from 'assert';
import findItemsOver20 from '../findItemsOver20.js';

describe('findItemsOver20', function() {
    

    it('should return an empty array if no items have qty greater than 20', function() {
        const fruits = [
            { name: 'apples', qty: 10 },
            { name: 'pears', qty: 15 },
        ];
       
    });
});
